# SPDX-FileCopyrightText: 2025 Deutsche Telekom IT GmbH
#
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: listeners.spectre.cp.ei.telekom.de
spec:
  group: spectre.cp.ei.telekom.de
  names:
    kind: Listener
    listKind: ListenerList
    plural: listeners
    singular: listener
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Listener is the Schema for the listeners API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListenerSpec defines the desired state of Listener.
            properties:
              apiListener:
                description: ApiListener configures a listener that proxies API requests.
                properties:
                  apiBasePath:
                    type: string
                  requestFilter:
                    description: ListenerFilter defines trigger and payload filtering
                      rules.
                    properties:
                      payload:
                        items:
                          type: string
                        type: array
                      trigger:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  responseFilter:
                    description: ListenerFilter defines trigger and payload filtering
                      rules.
                    properties:
                      payload:
                        items:
                          type: string
                        type: array
                      trigger:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                required:
                - apiBasePath
                type: object
              application:
                description: Application references the owning SpectreApplication
                  (set by the rover producer).
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              consumer:
                description: |-
                  TypedObjectRef is a reference to a Kubernetes object with type information
                  It is similar to ObjectRef but includes the Kind and APIVersion
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              eventListener:
                description: EventListener configures a listener that subscribes to
                  events.
                properties:
                  eventType:
                    type: string
                  filter:
                    description: ListenerFilter defines trigger and payload filtering
                      rules.
                    properties:
                      payload:
                        items:
                          type: string
                        type: array
                      trigger:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                required:
                - eventType
                type: object
              provider:
                description: |-
                  TypedObjectRef is a reference to a Kubernetes object with type information
                  It is similar to ObjectRef but includes the Kind and APIVersion
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - application
            - consumer
            - provider
            type: object
            x-kubernetes-validations:
            - message: apiListener is required
              rule: has(self.apiListener)
            - message: eventListener is not yet supported
              rule: '!has(self.eventListener)'
          status:
            description: ListenerStatus defines the observed state of Listener.
            properties:
              appliedPlacement:
                description: |-
                  AppliedListenerPlacementStatus records the durable placement state that was
                  last successfully reconciled. Controllers compare the current fingerprint
                  against the desired one to decide whether a re-placement is needed.
                properties:
                  callbackBaseURL:
                    type: string
                  callbackOriginZone:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  captureEventStore:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  captureRoute:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  captureZone:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  deliveryEventStore:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  deliveryZone:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  fingerprint:
                    type: string
                  publisher:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                type: object
              authorizationMigration:
                description: |-
                  AuthorizationMigrationStatus tracks the transition from a legacy
                  (provider-only) approval model to the scoped dual-gate model. Once the
                  migration completes, the controller sets AuthorizationPolicyVersion="v2"
                  on the parent ListenerStatus and clears this struct.
                properties:
                  drainStarted:
                    description: |-
                      DrainStarted is set to true when the migration initiates a drain.
                      It distinguishes "drain not yet started" (false) from "drain was
                      consumed by the outer handler" (true but Draining is nil).
                    type: boolean
                  legacyApproval:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  legacyRequests:
                    items:
                      description: |-
                        ObjectRef is a reference to a Kubernetes object
                        It is similar to types.NamespacedName but has the required json tags for serialization
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type: array
                  phase:
                    enum:
                    - Recorded
                    - Blocked
                    - AwaitingScoped
                    - Draining
                    - RetiringRequests
                    - RetiringApproval
                    type: string
                  retirementCheckpoint:
                    description: |-
                      MigrationRetirementCheckpoint records which legacy approval resources have
                      been retired so the controller can resume after a restart.
                    properties:
                      approvalRetired:
                        type: boolean
                      lastRetiredResourceVersion:
                        type: string
                      lastRetiredUID:
                        type: string
                      pendingDeletions:
                        items:
                          description: |-
                            PendingDeletion tracks per-resource deletion state so the controller can
                            survive a crash between issuing a delete and acknowledging the result.
                          properties:
                            kind:
                              type: string
                            name:
                              type: string
                            namespace:
                              type: string
                            phase:
                              enum:
                              - DeletePrepared
                              - DeleteObserved
                              type: string
                            resourceVersion:
                              type: string
                            uid:
                              type: string
                          type: object
                        type: array
                      requestsRetired:
                        type: boolean
                    type: object
                  targetPolicyVersion:
                    type: string
                required:
                - phase
                type: object
              authorizationPolicyVersion:
                description: |-
                  AuthorizationPolicyVersion records which approval model is active.
                  Set to "v2" only after a verified fresh-install or completed migration.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consumerApproval:
                description: |-
                  ObjectRef is a reference to a Kubernetes object
                  It is similar to types.NamespacedName but has the required json tags for serialization
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              consumerApprovalRequest:
                description: |-
                  ObjectRef is a reference to a Kubernetes object
                  It is similar to types.NamespacedName but has the required json tags for serialization
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              draining:
                description: |-
                  ListenerDrainStatus tracks the progress of tearing down an old placement.
                  The controller walks through the phases in order: stop new traffic, drain
                  subscribers, clean up the publisher, then mark complete.
                properties:
                  oldFingerprint:
                    type: string
                  oldRouteListener:
                    description: |-
                      OldRouteListener is the single-RouteListener record written by earlier
                      controller builds. It mirrors OldRouteListeners[0] and is still drained
                      when it is the only record.
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  oldRouteListeners:
                    description: |-
                      OldRouteListeners records every old-generation RouteListener that must be
                      gone before Subscribers drain, sorted by namespace, name, UID.
                    items:
                      description: |-
                        ObjectRef is a reference to a Kubernetes object
                        It is similar to types.NamespacedName but has the required json tags for serialization
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type: array
                  oldSubscribers:
                    items:
                      description: |-
                        ObjectRef is a reference to a Kubernetes object
                        It is similar to types.NamespacedName but has the required json tags for serialization
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type: array
                  phase:
                    enum:
                    - Stopping
                    - DrainingSubscribers
                    - CleaningPublisher
                    - Complete
                    type: string
                  reason:
                    type: string
                  sourceEventStore:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  sourcePublisher:
                    description: |-
                      ObjectRef is a reference to a Kubernetes object
                      It is similar to types.NamespacedName but has the required json tags for serialization
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  subscriptionIDs:
                    items:
                      type: string
                    type: array
                required:
                - phase
                type: object
              eventSubscriptions:
                items:
                  description: |-
                    ObjectRef is a reference to a Kubernetes object
                    It is similar to types.NamespacedName but has the required json tags for serialization
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              providerApproval:
                description: |-
                  ProviderApproval references the Approval that gates this Listener. The
                  provider owns the API whose traffic is captured, so its consent is the
                  security control. Only one approval can be modelled per owner CR — see
                  the note on ensureApprovals.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              providerApprovalRequest:
                description: |-
                  ObjectRef is a reference to a Kubernetes object
                  It is similar to types.NamespacedName but has the required json tags for serialization
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              routeListener:
                description: |-
                  ObjectRef is a reference to a Kubernetes object
                  It is similar to types.NamespacedName but has the required json tags for serialization
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
