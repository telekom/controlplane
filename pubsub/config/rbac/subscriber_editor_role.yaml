# Copyright 2026 Deutsche Telekom IT GmbH
#
# SPDX-License-Identifier: Apache-2.0

# This rule is not used by the project pubsub itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the pubsub.cp.ei.telekom.de.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: pubsub
    app.kubernetes.io/managed-by: kustomize
  name: subscriber-editor-role
rules:
- apiGroups:
  - pubsub.cp.ei.telekom.de
  resources:
  - subscribers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - pubsub.cp.ei.telekom.de
  resources:
  - subscribers/status
  verbs:
  - get
