# Copyright 2025 Deutsche Telekom IT GmbH
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spy-server-ingress
  namespace: system
  annotations:
spec:
  ingressClassName: nginx
  rules:
  - host: spy-server.example.com  # Example host that can be overridden via kustomize
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spy-server-service
            port:
              number: 8080
