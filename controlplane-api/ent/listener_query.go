// SPDX-FileCopyrightText: 2025 Deutsche Telekom IT GmbH
//
// SPDX-License-Identifier: Apache-2.0
// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/telekom/controlplane/controlplane-api/ent/apiexposure"
	"github.com/telekom/controlplane/controlplane-api/ent/apisubscription"
	"github.com/telekom/controlplane/controlplane-api/ent/approval"
	"github.com/telekom/controlplane/controlplane-api/ent/approvalrequest"
	"github.com/telekom/controlplane/controlplane-api/ent/listener"
	"github.com/telekom/controlplane/controlplane-api/ent/predicate"
)

// ListenerQuery is the builder for querying Listener entities.
type ListenerQuery struct {
	config
	ctx                       *QueryContext
	order                     []listener.OrderOption
	inters                    []Interceptor
	predicates                []predicate.Listener
	withSubscription          *ApiSubscriptionQuery
	withExposure              *ApiExposureQuery
	withProviderApproval      *ApprovalQuery
	withApprovalRequests      *ApprovalRequestQuery
	withFKs                   bool
	modifiers                 []func(*sql.Selector)
	loadTotal                 []func(context.Context, []*Listener) error
	withNamedApprovalRequests map[string]*ApprovalRequestQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ListenerQuery builder.
func (_q *ListenerQuery) Where(ps ...predicate.Listener) *ListenerQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ListenerQuery) Limit(limit int) *ListenerQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ListenerQuery) Offset(offset int) *ListenerQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ListenerQuery) Unique(unique bool) *ListenerQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ListenerQuery) Order(o ...listener.OrderOption) *ListenerQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySubscription chains the current query on the "subscription" edge.
func (_q *ListenerQuery) QuerySubscription() *ApiSubscriptionQuery {
	query := (&ApiSubscriptionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(listener.Table, listener.FieldID, selector),
			sqlgraph.To(apisubscription.Table, apisubscription.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, listener.SubscriptionTable, listener.SubscriptionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExposure chains the current query on the "exposure" edge.
func (_q *ListenerQuery) QueryExposure() *ApiExposureQuery {
	query := (&ApiExposureClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(listener.Table, listener.FieldID, selector),
			sqlgraph.To(apiexposure.Table, apiexposure.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, listener.ExposureTable, listener.ExposureColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProviderApproval chains the current query on the "provider_approval" edge.
func (_q *ListenerQuery) QueryProviderApproval() *ApprovalQuery {
	query := (&ApprovalClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(listener.Table, listener.FieldID, selector),
			sqlgraph.To(approval.Table, approval.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, listener.ProviderApprovalTable, listener.ProviderApprovalColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApprovalRequests chains the current query on the "approval_requests" edge.
func (_q *ListenerQuery) QueryApprovalRequests() *ApprovalRequestQuery {
	query := (&ApprovalRequestClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(listener.Table, listener.FieldID, selector),
			sqlgraph.To(approvalrequest.Table, approvalrequest.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, listener.ApprovalRequestsTable, listener.ApprovalRequestsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Listener entity from the query.
// Returns a *NotFoundError when no Listener was found.
func (_q *ListenerQuery) First(ctx context.Context) (*Listener, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{listener.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ListenerQuery) FirstX(ctx context.Context) *Listener {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Listener ID from the query.
// Returns a *NotFoundError when no Listener ID was found.
func (_q *ListenerQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{listener.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ListenerQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Listener entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Listener entity is found.
// Returns a *NotFoundError when no Listener entities are found.
func (_q *ListenerQuery) Only(ctx context.Context) (*Listener, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{listener.Label}
	default:
		return nil, &NotSingularError{listener.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ListenerQuery) OnlyX(ctx context.Context) *Listener {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Listener ID in the query.
// Returns a *NotSingularError when more than one Listener ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ListenerQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{listener.Label}
	default:
		err = &NotSingularError{listener.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ListenerQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Listeners.
func (_q *ListenerQuery) All(ctx context.Context) ([]*Listener, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Listener, *ListenerQuery]()
	return withInterceptors[[]*Listener](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ListenerQuery) AllX(ctx context.Context) []*Listener {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Listener IDs.
func (_q *ListenerQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(listener.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ListenerQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ListenerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ListenerQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ListenerQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ListenerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ListenerQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ListenerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ListenerQuery) Clone() *ListenerQuery {
	if _q == nil {
		return nil
	}
	return &ListenerQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]listener.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.Listener{}, _q.predicates...),
		withSubscription:     _q.withSubscription.Clone(),
		withExposure:         _q.withExposure.Clone(),
		withProviderApproval: _q.withProviderApproval.Clone(),
		withApprovalRequests: _q.withApprovalRequests.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSubscription tells the query-builder to eager-load the nodes that are connected to
// the "subscription" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ListenerQuery) WithSubscription(opts ...func(*ApiSubscriptionQuery)) *ListenerQuery {
	query := (&ApiSubscriptionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSubscription = query
	return _q
}

// WithExposure tells the query-builder to eager-load the nodes that are connected to
// the "exposure" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ListenerQuery) WithExposure(opts ...func(*ApiExposureQuery)) *ListenerQuery {
	query := (&ApiExposureClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExposure = query
	return _q
}

// WithProviderApproval tells the query-builder to eager-load the nodes that are connected to
// the "provider_approval" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ListenerQuery) WithProviderApproval(opts ...func(*ApprovalQuery)) *ListenerQuery {
	query := (&ApprovalClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProviderApproval = query
	return _q
}

// WithApprovalRequests tells the query-builder to eager-load the nodes that are connected to
// the "approval_requests" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ListenerQuery) WithApprovalRequests(opts ...func(*ApprovalRequestQuery)) *ListenerQuery {
	query := (&ApprovalRequestClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withApprovalRequests = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Listener.Query().
//		GroupBy(listener.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ListenerQuery) GroupBy(field string, fields ...string) *ListenerGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ListenerGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = listener.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Listener.Query().
//		Select(listener.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *ListenerQuery) Select(fields ...string) *ListenerSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ListenerSelect{ListenerQuery: _q}
	sbuild.label = listener.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ListenerSelect configured with the given aggregations.
func (_q *ListenerQuery) Aggregate(fns ...AggregateFunc) *ListenerSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ListenerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !listener.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if listener.Policy == nil {
		return errors.New("ent: uninitialized listener.Policy (forgotten import ent/runtime?)")
	}
	if err := listener.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *ListenerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Listener, error) {
	var (
		nodes       = []*Listener{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withSubscription != nil,
			_q.withExposure != nil,
			_q.withProviderApproval != nil,
			_q.withApprovalRequests != nil,
		}
	)
	if _q.withSubscription != nil || _q.withExposure != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, listener.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Listener).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Listener{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSubscription; query != nil {
		if err := _q.loadSubscription(ctx, query, nodes, nil,
			func(n *Listener, e *ApiSubscription) { n.Edges.Subscription = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExposure; query != nil {
		if err := _q.loadExposure(ctx, query, nodes, nil,
			func(n *Listener, e *ApiExposure) { n.Edges.Exposure = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withProviderApproval; query != nil {
		if err := _q.loadProviderApproval(ctx, query, nodes, nil,
			func(n *Listener, e *Approval) { n.Edges.ProviderApproval = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withApprovalRequests; query != nil {
		if err := _q.loadApprovalRequests(ctx, query, nodes,
			func(n *Listener) { n.Edges.ApprovalRequests = []*ApprovalRequest{} },
			func(n *Listener, e *ApprovalRequest) { n.Edges.ApprovalRequests = append(n.Edges.ApprovalRequests, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedApprovalRequests {
		if err := _q.loadApprovalRequests(ctx, query, nodes,
			func(n *Listener) { n.appendNamedApprovalRequests(name) },
			func(n *Listener, e *ApprovalRequest) { n.appendNamedApprovalRequests(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ListenerQuery) loadSubscription(ctx context.Context, query *ApiSubscriptionQuery, nodes []*Listener, init func(*Listener), assign func(*Listener, *ApiSubscription)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Listener)
	for i := range nodes {
		if nodes[i].api_subscription_listeners == nil {
			continue
		}
		fk := *nodes[i].api_subscription_listeners
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(apisubscription.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "api_subscription_listeners" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ListenerQuery) loadExposure(ctx context.Context, query *ApiExposureQuery, nodes []*Listener, init func(*Listener), assign func(*Listener, *ApiExposure)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Listener)
	for i := range nodes {
		if nodes[i].api_exposure_listeners == nil {
			continue
		}
		fk := *nodes[i].api_exposure_listeners
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(apiexposure.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "api_exposure_listeners" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ListenerQuery) loadProviderApproval(ctx context.Context, query *ApprovalQuery, nodes []*Listener, init func(*Listener), assign func(*Listener, *Approval)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Listener)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.Approval(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(listener.ProviderApprovalColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.listener_provider_approval
		if fk == nil {
			return fmt.Errorf(`foreign-key "listener_provider_approval" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "listener_provider_approval" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ListenerQuery) loadApprovalRequests(ctx context.Context, query *ApprovalRequestQuery, nodes []*Listener, init func(*Listener), assign func(*Listener, *ApprovalRequest)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Listener)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ApprovalRequest(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(listener.ApprovalRequestsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.listener_approval_requests
		if fk == nil {
			return fmt.Errorf(`foreign-key "listener_approval_requests" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "listener_approval_requests" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ListenerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ListenerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(listener.Table, listener.Columns, sqlgraph.NewFieldSpec(listener.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, listener.FieldID)
		for i := range fields {
			if fields[i] != listener.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ListenerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(listener.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = listener.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedApprovalRequests tells the query-builder to eager-load the nodes that are connected to the "approval_requests"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *ListenerQuery) WithNamedApprovalRequests(name string, opts ...func(*ApprovalRequestQuery)) *ListenerQuery {
	query := (&ApprovalRequestClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedApprovalRequests == nil {
		_q.withNamedApprovalRequests = make(map[string]*ApprovalRequestQuery)
	}
	_q.withNamedApprovalRequests[name] = query
	return _q
}

// ListenerGroupBy is the group-by builder for Listener entities.
type ListenerGroupBy struct {
	selector
	build *ListenerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ListenerGroupBy) Aggregate(fns ...AggregateFunc) *ListenerGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ListenerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ListenerQuery, *ListenerGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ListenerGroupBy) sqlScan(ctx context.Context, root *ListenerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ListenerSelect is the builder for selecting fields of Listener entities.
type ListenerSelect struct {
	*ListenerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ListenerSelect) Aggregate(fns ...AggregateFunc) *ListenerSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ListenerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ListenerQuery, *ListenerSelect](ctx, _s.ListenerQuery, _s, _s.inters, v)
}

func (_s *ListenerSelect) sqlScan(ctx context.Context, root *ListenerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
