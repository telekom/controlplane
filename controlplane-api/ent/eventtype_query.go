// SPDX-FileCopyrightText: 2025 Deutsche Telekom IT GmbH
//
// SPDX-License-Identifier: Apache-2.0
// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/telekom/controlplane/controlplane-api/ent/eventexposure"
	"github.com/telekom/controlplane/controlplane-api/ent/eventtype"
	"github.com/telekom/controlplane/controlplane-api/ent/predicate"
	"github.com/telekom/controlplane/controlplane-api/ent/team"
)

// EventTypeQuery is the builder for querying EventType entities.
type EventTypeQuery struct {
	config
	ctx                *QueryContext
	order              []eventtype.OrderOption
	inters             []Interceptor
	predicates         []predicate.EventType
	withOwner          *TeamQuery
	withExposures      *EventExposureQuery
	withFKs            bool
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*EventType) error
	withNamedExposures map[string]*EventExposureQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EventTypeQuery builder.
func (_q *EventTypeQuery) Where(ps ...predicate.EventType) *EventTypeQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *EventTypeQuery) Limit(limit int) *EventTypeQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *EventTypeQuery) Offset(offset int) *EventTypeQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *EventTypeQuery) Unique(unique bool) *EventTypeQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *EventTypeQuery) Order(o ...eventtype.OrderOption) *EventTypeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOwner chains the current query on the "owner" edge.
func (_q *EventTypeQuery) QueryOwner() *TeamQuery {
	query := (&TeamClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(eventtype.Table, eventtype.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, eventtype.OwnerTable, eventtype.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExposures chains the current query on the "exposures" edge.
func (_q *EventTypeQuery) QueryExposures() *EventExposureQuery {
	query := (&EventExposureClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(eventtype.Table, eventtype.FieldID, selector),
			sqlgraph.To(eventexposure.Table, eventexposure.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, eventtype.ExposuresTable, eventtype.ExposuresColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EventType entity from the query.
// Returns a *NotFoundError when no EventType was found.
func (_q *EventTypeQuery) First(ctx context.Context) (*EventType, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{eventtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *EventTypeQuery) FirstX(ctx context.Context) *EventType {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EventType ID from the query.
// Returns a *NotFoundError when no EventType ID was found.
func (_q *EventTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{eventtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *EventTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EventType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EventType entity is found.
// Returns a *NotFoundError when no EventType entities are found.
func (_q *EventTypeQuery) Only(ctx context.Context) (*EventType, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{eventtype.Label}
	default:
		return nil, &NotSingularError{eventtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *EventTypeQuery) OnlyX(ctx context.Context) *EventType {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EventType ID in the query.
// Returns a *NotSingularError when more than one EventType ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *EventTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{eventtype.Label}
	default:
		err = &NotSingularError{eventtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *EventTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EventTypes.
func (_q *EventTypeQuery) All(ctx context.Context) ([]*EventType, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EventType, *EventTypeQuery]()
	return withInterceptors[[]*EventType](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *EventTypeQuery) AllX(ctx context.Context) []*EventType {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EventType IDs.
func (_q *EventTypeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(eventtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *EventTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *EventTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*EventTypeQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *EventTypeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *EventTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *EventTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EventTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *EventTypeQuery) Clone() *EventTypeQuery {
	if _q == nil {
		return nil
	}
	return &EventTypeQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]eventtype.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.EventType{}, _q.predicates...),
		withOwner:     _q.withOwner.Clone(),
		withExposures: _q.withExposures.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EventTypeQuery) WithOwner(opts ...func(*TeamQuery)) *EventTypeQuery {
	query := (&TeamClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOwner = query
	return _q
}

// WithExposures tells the query-builder to eager-load the nodes that are connected to
// the "exposures" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EventTypeQuery) WithExposures(opts ...func(*EventExposureQuery)) *EventTypeQuery {
	query := (&EventExposureClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExposures = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EventType.Query().
//		GroupBy(eventtype.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *EventTypeQuery) GroupBy(field string, fields ...string) *EventTypeGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EventTypeGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = eventtype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EventType.Query().
//		Select(eventtype.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *EventTypeQuery) Select(fields ...string) *EventTypeSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &EventTypeSelect{EventTypeQuery: _q}
	sbuild.label = eventtype.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EventTypeSelect configured with the given aggregations.
func (_q *EventTypeQuery) Aggregate(fns ...AggregateFunc) *EventTypeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *EventTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !eventtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if eventtype.Policy == nil {
		return errors.New("ent: uninitialized eventtype.Policy (forgotten import ent/runtime?)")
	}
	if err := eventtype.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *EventTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EventType, error) {
	var (
		nodes       = []*EventType{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withOwner != nil,
			_q.withExposures != nil,
		}
	)
	if _q.withOwner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, eventtype.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EventType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EventType{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOwner; query != nil {
		if err := _q.loadOwner(ctx, query, nodes, nil,
			func(n *EventType, e *Team) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExposures; query != nil {
		if err := _q.loadExposures(ctx, query, nodes,
			func(n *EventType) { n.Edges.Exposures = []*EventExposure{} },
			func(n *EventType, e *EventExposure) { n.Edges.Exposures = append(n.Edges.Exposures, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedExposures {
		if err := _q.loadExposures(ctx, query, nodes,
			func(n *EventType) { n.appendNamedExposures(name) },
			func(n *EventType, e *EventExposure) { n.appendNamedExposures(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *EventTypeQuery) loadOwner(ctx context.Context, query *TeamQuery, nodes []*EventType, init func(*EventType), assign func(*EventType, *Team)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EventType)
	for i := range nodes {
		if nodes[i].team_event_types == nil {
			continue
		}
		fk := *nodes[i].team_event_types
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "team_event_types" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *EventTypeQuery) loadExposures(ctx context.Context, query *EventExposureQuery, nodes []*EventType, init func(*EventType), assign func(*EventType, *EventExposure)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*EventType)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.EventExposure(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(eventtype.ExposuresColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.event_type_exposures
		if fk == nil {
			return fmt.Errorf(`foreign-key "event_type_exposures" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "event_type_exposures" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *EventTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *EventTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(eventtype.Table, eventtype.Columns, sqlgraph.NewFieldSpec(eventtype.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, eventtype.FieldID)
		for i := range fields {
			if fields[i] != eventtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *EventTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(eventtype.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = eventtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedExposures tells the query-builder to eager-load the nodes that are connected to the "exposures"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *EventTypeQuery) WithNamedExposures(name string, opts ...func(*EventExposureQuery)) *EventTypeQuery {
	query := (&EventExposureClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedExposures == nil {
		_q.withNamedExposures = make(map[string]*EventExposureQuery)
	}
	_q.withNamedExposures[name] = query
	return _q
}

// EventTypeGroupBy is the group-by builder for EventType entities.
type EventTypeGroupBy struct {
	selector
	build *EventTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *EventTypeGroupBy) Aggregate(fns ...AggregateFunc) *EventTypeGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *EventTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EventTypeQuery, *EventTypeGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *EventTypeGroupBy) sqlScan(ctx context.Context, root *EventTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EventTypeSelect is the builder for selecting fields of EventType entities.
type EventTypeSelect struct {
	*EventTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *EventTypeSelect) Aggregate(fns ...AggregateFunc) *EventTypeSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *EventTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EventTypeQuery, *EventTypeSelect](ctx, _s.EventTypeQuery, _s, _s.inters, v)
}

func (_s *EventTypeSelect) sqlScan(ctx context.Context, root *EventTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
