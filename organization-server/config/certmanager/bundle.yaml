# Copyright 2025 Deutsche Telekom IT GmbH
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: controlplane-api-trust-bundle
spec:
  sources:
  - secret:
      name: controlplane-api-cert
      key: ca.crt
  target:
    configMap:
      key: trust-bundle.pem
    namespaceSelector:
      matchLabels:
        cp.ei.telekom.de/controlplane-api: enabled
