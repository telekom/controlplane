# SPDX-FileCopyrightText: 2025 Deutsche Telekom IT GmbH
#
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: eventconfigs.event.cp.ei.telekom.de
spec:
  group: event.cp.ei.telekom.de
  names:
    kind: EventConfig
    listKind: EventConfigList
    plural: eventconfigs
    singular: eventconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Zone
      jsonPath: .spec.zone.name
      name: Zone
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          EventConfig is the Schema for the eventconfigs API.
          It provides configuration for the event operator, including the configuration backend
          connection and OAuth2 authentication settings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventConfigSpec defines the desired state of EventConfig.
            properties:
              admin:
                description: Admin configures the connection to the configuration
                  backend.
                properties:
                  client:
                    description: Client configures the identity client used for admin
                      access to the configuration backend.
                    properties:
                      clientId:
                        description: |-
                          ClientId is the OAuth2 client ID for authentication
                          If empty, a default client ID will be used.
                        type: string
                      clientSecret:
                        description: |-
                          ClientSecret is the OAuth2 client secret for authentication
                          If empty, a new secret will be generated and a reference to it will be stored in the EventConfig status.
                        type: string
                      realm:
                        description: |-
                          Realm references the identity Realm CR used for OAuth2 authentication
                          If empty, it is assumed that the default realm should be used.
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                          uid:
                            description: |-
                              UID is a type that holds unique ID values, including UUIDs.  Because we
                              don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                              intent and helps make sure that UIDs and names do not get conflated.
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    type: object
                  url:
                    description: Url is the base URL of the configuration backend
                      API.
                    format: uri
                    minLength: 1
                    type: string
                required:
                - client
                - url
                type: object
              mesh:
                description: Mesh configures the mesh topology for event distribution.
                properties:
                  client:
                    description: Client configures the identity client used for mesh
                      communication between zones.
                    properties:
                      clientId:
                        description: |-
                          ClientId is the OAuth2 client ID for authentication
                          If empty, a default client ID will be used.
                        type: string
                      clientSecret:
                        description: |-
                          ClientSecret is the OAuth2 client secret for authentication
                          If empty, a new secret will be generated and a reference to it will be stored in the EventConfig status.
                        type: string
                      realm:
                        description: |-
                          Realm references the identity Realm CR used for OAuth2 authentication
                          If empty, it is assumed that the default realm should be used.
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                          uid:
                            description: |-
                              UID is a type that holds unique ID values, including UUIDs.  Because we
                              don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                              intent and helps make sure that UIDs and names do not get conflated.
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    type: object
                  fullMesh:
                    default: true
                    description: FullMesh enables a full mesh topology where events
                      are distributed to all zones.
                    type: boolean
                  zoneNames:
                    description: |-
                      ZoneNames lists specific zones for event distribution in a partial mesh topology.
                      Must be set if FullMesh is false.
                    items:
                      type: string
                    type: array
                required:
                - client
                - fullMesh
                type: object
              publishEventUrl:
                description: |-
                  PublishEventUrl is the internal URL of the publish backend service
                  Used as the upstream for the publish gateway Route.
                format: uri
                type: string
              serverSendEventUrl:
                description: |-
                  ServerSendEventUrl is the internal URL of the SSE backend service
                  Used as the upstream for the SSE gateway Route.
                format: uri
                type: string
              voyagerApiUrl:
                description: |-
                  VoyagerApiUrl is the internal URL of the Voyager backend service.
                  Used as the upstream for the Voyager gateway Route which exposes
                  event listing and redelivery APIs.
                format: uri
                type: string
              zone:
                description: Zone references the Zone for which this EventConfig applies.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - admin
            - mesh
            - publishEventUrl
            - serverSendEventUrl
            - zone
            type: object
          status:
            description: EventConfigStatus defines the observed state of EventConfig.
            properties:
              adminClient:
                description: AdminClient references the identity Client CR created
                  for admin access to the configuration backend.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the generation of the referenced
                      object that has been observed by the controller.
                    format: int64
                    type: integer
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                - observedGeneration
                type: object
              callbackRoute:
                description: CallbackRoute references the Route CR created for the
                  callback gateway.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              callbackUrl:
                description: CallbackURL is the external URL of the callback gateway,
                  used to send events to event consumers.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eventStore:
                description: EventStore references the EventStore CR in the pubsub
                  domain.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              meshClient:
                description: MeshClient references the identity Client CR created
                  for mesh access between zones.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the generation of the referenced
                      object that has been observed by the controller.
                    format: int64
                    type: integer
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                - observedGeneration
                type: object
              proxyCallbackRoutes:
                description: ProxyCallbackRoutes references the Route CRs created
                  for the proxy callback gateway.
                items:
                  description: |-
                    ObjectRef is a reference to a Kubernetes object
                    It is similar to types.NamespacedName but has the required json tags for serialization
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              proxyCallbackUrls:
                additionalProperties:
                  type: string
                description: |-
                  ProxyCallbackURLs maps zone names to the external URLs of the proxy callback gateway Routes for those zones.
                  Used to send events to event consumers in other zones.
                type: object
              proxyVoyagerRoutes:
                description: ProxyVoyagerRoutes references the proxy Route CRs created
                  for cross-zone Voyager access.
                items:
                  description: |-
                    ObjectRef is a reference to a Kubernetes object
                    It is similar to types.NamespacedName but has the required json tags for serialization
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              proxyVoyagerUrls:
                additionalProperties:
                  type: string
                description: ProxyVoyagerURLs maps zone names to the external URLs
                  of the proxy Voyager gateway Routes for those zones.
                type: object
              publishRoute:
                description: PublishRoute references the Route CR created for the
                  publish gateway.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              publishUrl:
                description: PublishURL is the external URL of the publish gateway,
                  used by event producers to publish events.
                type: string
              voyagerRoute:
                description: VoyagerRoute references the primary Route CR created
                  for the Voyager gateway.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                - namespace
                type: object
              voyagerUrl:
                description: |-
                  VoyagerURL is the external gateway URL for the Voyager API,
                  used for event listing and redelivery.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
